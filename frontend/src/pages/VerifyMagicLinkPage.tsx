import type { JSX } from "react";

const VerifyMagicLinkPage = () => {
  // This page will extract the token from URL and call the backend
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold">Verifying Magic Link...</h1>
      {/* Logic to handle token verification */}
    </div>
  );
};

export default VerifyMagicLinkPage;
